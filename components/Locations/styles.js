import { makeStyles } from "@material-ui/core";

export default (theme) => ({
  root: {},
  main: {
    backgroundColor: "#ffffff",
    height: "inherit",
  },
  hiddenIcon: {
    visibility: 'hidden',
  },
});

