import React from "react";
import Layout from "../layouts/Layout";
import MenuList from "../layouts/MenuList";

const Menu = () => {
  return (
    <Layout>
      <MenuList />
    </Layout>
  );
};

export default Menu;
